# Application
APP_PORT=8080
APP_ENV=development

# PostgreSQL
DATABASE_URL=postgres://konfig:konfig@localhost:5432/settings_db
DATABASE_MAX_CONNECTIONS=10

# Internal auth — must match INTERNAL_API_SECRET in konfig-facade
INTERNAL_API_SECRET=a-strong-random-secret-min-32-chars
